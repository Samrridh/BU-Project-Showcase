new
 can show data in realtime - only in memory storage
- button to locate the lost item


backend and frontend 
saves image files but persistant disk is paid in render so dropping it
-button to mark it as completed